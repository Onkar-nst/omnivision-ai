--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.2
torchvision>=0.17.2

# YOLOv8 — pins ultralytics to a version known-good on Py 3.11 + CPU torch
ultralytics>=8.3.0

# OpenCV headless (no GUI dependencies — required on server environments)
opencv-python-headless>=4.8.0

# Image processing
Pillow>=10.4.0
numpy>=1.26.0

# ── Backend framework ───────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9      # needed for File upload parsing

# ── Groq AI summary ────────────────────────────────────────
groq>=0.9.0

# ── Utilities ──────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.0.0
